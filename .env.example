# Copier en .env (jamais commité) avant de compiler : .env est embarqué
# dans l'application (pubspec.yaml, assets). N'y mettre que des valeurs
# publiques par conception — jamais la clé secrète ni service_role.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
