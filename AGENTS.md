# AGENTS.md — Feuille de route de développement

> Fichier de référence pour **tout agent de codage** (humain ou IA) travaillant sur Ecclésias360.
> À lire **en premier** avant toute tâche, et à **mettre à jour** à chaque module livré.
> Source de vérité métier : `Cahier_de_Conception_Ecclesias360_v2.1.md` (règles `RG-*`).
>
> **Note de reconstruction (2026-09)** : ce dépôt repart de zéro après perte totale du disque de
> développement précédent. `RECONSTRUCTION_ecclesias360.md` contient la spécification technique
> intégrale de tout ce qui avait été construit au-delà du Cahier (CV Ecclésiastique, i18n, Bible
> Paliers 1-2 + multi-versions, Cantiques, Église Sœur) — ce sont des décisions déjà tranchées,
> à réappliquer telles quelles dès que leurs modules prérequis existent, pas à redébattre.

---

## 1. Identité du projet

- **Nom** : Ecclésias360 — plateforme intégrée de gestion ecclésiastique.
- **Client** : Global Service Groupe.
- **Package Dart** : `ecclesias_360`.
- **Dart SDK** : `^3.12.2` · **Flutter** avec `flutter_lints ^6.0.0`.
- **Langue du code et des commits** : français.

---

## 2. Stack technique

| Domaine | Choix | Remarque |
|---|---|---|
| UI / état | Flutter, Material 3, `provider` (ChangeNotifier) | Un `Controller` par module |
| Routage | `go_router` | Routes centralisées dans `core/router/app_router.dart` |
| Persistance locale | `drift` + `sqlite3_flutter_libs` | Base **offline-first** unique |
| Backend | Supabase (`supabase_flutter ^2.17.2`) | Clé via `publishableKey` (et non `anonKey`, dépréciée) |
| Config / secrets | `flutter_dotenv` + `.env` | `.env` **jamais commité** |
| Stockage sensible | `flutter_secure_storage` | Jetons, secrets |
| Codegen | `build_runner` + `drift_dev` | Génère les classes `*Row` et `*Companion` |
| Analyse | `flutter analyze` (flutter_lints) | **Zéro issue** exigé avant commit |
| Tests | `flutter test` | Règles métier = tests unitaires purs |

---

## 3. Architecture (feature-first / clean)

```
domain/        → logique pure, SANS dépendance Flutter/Drift/Supabase (testable)
  models/      → entités, enums, DTO
  rules/       → règles métier RG-* (fonctions/classes pures)
data/
  local/       → tables Drift + requêtes
  remote/      → sources de données Supabase
  referential/ → chargeurs JSON de assets/config/
  <module>_repository.dart → écriture locale + outbox + sync
application/
  <module>_controller.dart → ChangeNotifier exposé aux écrans
presentation/
  *.dart        → écrans (un fichier par écran)
```

**Règle de dépendance** : `presentation → application → data → domain`, jamais l'inverse.
Les règles de `domain/rules` ne doivent référencer ni `BuildContext`, ni Drift, ni Supabase.

---

## 4. Structure des répertoires

```
lib/
  main.dart
  app.dart
  l10n/
    app_fr.arb   app_en.arb   app_es.arb   app_pt.arb
  core/
    config/app_config.dart
    constants/app_routes.dart
    theme/
    router/app_router.dart
    utils/id_generator.dart
    ai/
    widgets/icon_catalog.dart
  features/
    auth/ onboarding/ home/
    organization/ fideles/ ministries/ dons_spirituels/ professions/
    groupes_eglise/ comite/ archivage/ deplacements/ discipline/
    finances/ presences/ evenements/ patrimoine/ mediatheque/
assets/
  config/   fonts/
supabase/migrations/
test/
```

Tables Drift centralisées dans `lib/features/organization/data/local/`
(`tables.dart` + `app_database.dart` + `app_database.g.dart`), partagées par tous les modules.

Il n'existe **aucun fichier `core/constants/app_strings.dart`** : les chaînes UI passent par le
système ARB / `l10n.*` dès l'origine (voir §5) — ne pas recréer ce fichier.

---

## 5. Conventions de code

- Nommage : PascalCase classes/widgets, camelCase méthodes/variables, snake_case fichiers.
- Chaînes UI : jamais en dur → système ARB / `l10n.*`, aucun fichier de constantes intermédiaire
  (pas de `app_strings.dart` — supprimé dans la session précédente pour cette raison, ne pas le
  réintroduire, même temporairement).
- Routes : jamais en dur → `core/constants/app_routes.dart`.
- Valeurs IA : zéro valeur en dur → `assets/config/ai_roles.json`.
- Typage : éviter `dynamic`.
- Async : `context.mounted` après tout `await` avant usage de `context`.
- Thème — chaque sorte d'expression a un seul foyer, jamais de deuxième
  fichier de constantes qui ferait doublon :

  | Sorte d'expression | Foyer |
  |---|---|
  | Couleurs | `core/theme/app_palettes.dart` |
  | Espacements, rayons, tailles, durées | `core/theme/app_dimensions.dart` |
  | Valeurs par défaut des règles (non imposées par le Cahier) | `core/theme/app_defaults.dart` |
  | Pays, devise, langue | référentiel du Kernel (voir §11) |
  | Assemblage `ThemeData` (Material) | `core/theme/design_tokens.dart` |

  Deux tests (`test/core/theme/`) interdisent les couleurs en dur dans tout
  `lib/`, et les nombres de mise en page en dur dans les écrans déjà migrés
  vers `AppDimensions` (liste dans le test lui-même, à étendre à chaque
  écran migré). Un test de contraste (`app_palettes_contrast_test.dart`)
  vérifie chaque palette actuelle et future contre le Cahier §8.3
  (lisibilité en plein soleil) : WCAG AA texte normal (4.5:1) pour le texte
  courant, WCAG AA texte large (3:1) pour un libellé sur fond coloré
  (bouton, badge).
- Offline-first (RG-OFF) : écriture Drift d'abord, puis outbox, sync Supabase secondaire et jamais bloquante.
- `flutter analyze` → **No issues found!** exigé.

---

## 6. Agents IA (pool fermé de noms bibliques/angéliques)

| Nom | Rôle | Statut |
|---|---|---|
| Ezra | Greffier des fidèles (RG-II-06) | actif |
| Nathan | Assistant pastoral (RG-II-02) | actif |
| Dina | Dédoublonnage / import (RG-II-09) | actif |
| Uriel | Veilleur RGPD (RG-II-05/10) | actif |
| Lailah | Conseiller d'affectation ministères (RG-IV-03) | actif |
| Logos | Réservé (Module XVI) | réservé |
| Charmeine | Réservé (Module XVI) | réservé |

Garde-fous : les IA ne modifient jamais les données directement — suggestions uniquement,
confirmation utilisateur, exécution par le service métier normal, traçée. Chaque rôle déclare
`permission` (`suggest`/`propose`), `scope`, `forbiddenFields` dans `assets/config/ai_roles.json`.

**Donnée interdite à tout rôle IA, sans exception (Nathan compris)** : les notes pastorales
privées (`notes_pastorales`, RG-II-11) et leur journal de consultations. À déclarer dans
`forbiddenFields` de chaque rôle dès que `ai_roles.json` sera créé (Module XVI) — un assistant
qui lirait ces notes les exposerait hors de la règle d'accès et hors du journal.

---

## 7. État d'avancement des modules

> Repli à zéro (2026-09) : ce tableau reflète l'état réel du dépôt reconstruit, pas l'état
> atteint avant le sinistre. Ordre canonique = Annexe D du Cahier (voir §8 « Phases » ci-dessous).
> Pour les modules/extensions déjà spécifiés en détail dans `RECONSTRUCTION_ecclesias360.md`
> (CV, Bible multi-versions, Cantiques, Église Sœur, sécurité RG-SEC-05), la spécification est
> **déjà tranchée** — seule l'implémentation reste à faire, dès que les prérequis existent.

| Module (n° Cahier) | Statut |
|---|---|
| Socle transversal (Phase 1) | ⚠️ partiel — `AppDatabase` (Drift), `IdGenerator`, `SyncCoordinator` (outbox RG-OFF-02) livrés. Sécurité de base (RG-SEC-05) : helpers `security definer` `fidele_courant_id()`/`role_courant()`/`noeuds_du_perimetre()` (search_path durci sur `pg_catalog, pg_temp`) livrés et vérifiés par exécution réelle (PGlite + stub du schéma `auth`), miroir Dart `PerimetreRules` testé en isolation. policies RLS **actives** sur les 67 tables depuis `0019_activation_policies_rls.sql` (168 policies, gabarits périmètre/propriétaire/confidentialité relationnelle/rôle/référentiel, administrateur non borné), vérifiées par `supabase/tests/0019_activation_policies_rls_test.sql` (9 profils réels) et rapport table par table `supabase/tests/0019_rapport_rls.md` ; **RG-SEC-01 (authentification Supabase native sans mot de passe) livré** : module `features/auth` (SMS/WhatsApp/Magic Link/code e-mail via `signInWithOtp`/`verifyOtp`, validation E.164 côté client KER-ID-06, session dans `flutter_secure_storage` restaurée hors ligne RG-OFF, `SessionController` à la racine dont la `garde` (état + rôle seulement) est le `refreshListenable` du routeur — le rafraîchir à chaque écriture sur `fideles` restaurait une page dépilée) ; liaison compte ↔ fiche par correspondance automatique téléphone/e-mail avec garde-fou recyclage de numéros (fiche déjà liée, même par le passé → utilisateur simple + conflit journalisé à résoudre par un administrateur, écran « Liaisons de comptes ») ; un administrateur sans fiche (amorçage) se lie à sa propre fiche (`CompteRepository.lierMonCompteAFiche`, action sur la fiche fidèle, miroir serveur `0020_liaison_administrateur.sql` testé par `supabase/tests/0020_liaison_administrateur_test.sql`) : jamais sur une fiche liée maintenant ou par le passé, rôle administrateur conservé, journalisé comme résolution — toute validation (Modules XI, XX) trace ainsi une personne du registre, jamais un compte technique ; premier compte sur base vide = administrateur d'amorçage ; miroir serveur `0018_authentification.sql` (`enregistrer_compte_courant()`, `resoudre_conflit_liaison()`, `role_courant()` étendu) vérifié par `supabase/tests/0018_authentification_test.sql` contre la stack locale ; vérification par exécution réelle : `integration_test/connexion_otp_reelle_test.dart` (`-d windows`, stack `supabase start`, code e-mail relevé dans Mailpit, numéro SMS de test de `config.toml`) — a révélé que `.env` n'était pas embarqué (désormais déclaré en asset, sans lui l'accès reste fermé). **RG-SEC-01bis (GSG ID) reporté** : le GSG Platform Kernel n'est configuré sur aucun environnement accessible (aucune URL/clé Kernel, ni ici ni dans EcoShop) ; 5 palettes de thème (Cahier §8.3, maquette visuelle) livrées dans `app_palettes.dart` avec correction de contraste documentée par palette, jetons d'espacement (`app_dimensions.dart`) et de valeurs par défaut (`app_defaults.dart`) — voir §5 ; écrans Organisation et accueil migrés vers ces jetons ; pas de sélecteur de thème (décision produit non tranchée par le Cahier, non construite) ; 88 chaînes des écrans Fidèles et Comité migrées vers `l10n/app_*.arb` (fr/en/es/pt), 2 jetons supplémentaires (`labelColumnWidthNarrow`, `dividerHairline`) ajoutés à `app_dimensions.dart` pour ces écrans; reprise du design visuel de la maquette Rocket (`ecclesias360_Nouveau`, référence graphique uniquement — données fictives, aucune logique métier) amorcée : typographie DM Sans (`google_fonts`) et élévation/ombre différenciée clair/sombre ajoutées à `design_tokens.dart`, coquille de navigation à 4 onglets (`core/widgets/app_shell.dart`, `NavigationBar`) branchée en `ShellRoute` sur les 4 écrans racines sans paramètre (Accueil, Organisation, Fidèles, Paramètres — nouvel écran `parametres_screen.dart`), écran d'accueil reconstruit en tableau de bord (grille de tuiles de modules) façon `dashboard_screen.dart` de la maquette. Portée volontairement limitée aux écrans déjà livrés : les ~20 écrans de la maquette sans module métier construit (Cultes, Finances, Comptabilité, Bible, CV Ecclésiastique, Église Sœur, Messagerie...) ne sont pas repris avant que leur module existe (logique avant design) |
| I — Organisation | ⚠️ partiel — Lot 1+2 (Domaine, Données, Contrôleur, écrans) + Lot 3 (`ResponsableNoeud`/`node_responsables`, RG-I-05) livrés : 9/9 points d'écran mobile du Cahier couverts (7 écrans réels — arbre avec recherche intégrée, fiche avec statistiques rapides intégrées, création/édition, historique des rattachements, annuaire des Églises, liste des responsables + affectation). Éditions Windows dédiées (layout multi-colonnes) : **non bloquées par un module** — choix délibéré de différer (mêmes écrans que mobile pour l'instant), reprise possible dès que priorisé. **Droits (lot « droits I/II/XXIII », 2026-09-24)** — ✅ `OrganisationAccesRules`, miroir des policies `organisation_nodes`/`node_responsables` de 0019 : arbre complet au rang responsable (approximation du périmètre, dette RG-SEC-05) ; un fidèle enregistré ne voit que **son église** (jamais ses nœuds enfants), un utilisateur simple aucun nœud (RG-SEC-06bis — il ouvrait jusqu'ici tout l'arbre) ; fiche de nœud gardée avant l'existence, y compris par la route ; modification, archivage, nœud enfant, historique des rattachements et liste des responsables au rang responsable ; désignation/retrait d'un responsable au rang pasteur ; **RG-I-03** : créer ou valider un nœud d'un niveau supérieur à l'église locale (racine comprise) exige la capacité `creer_noeud_niveau_superieur`, le formulaire ne propose que les types permis ; annuaire des Églises (RG-I-09) derrière `consulter_annuaire_eglises`. Ces contrôles sont appliqués **à la source**, dans `OrganisationController` (même niveau que le Module II) : chaque écriture reçoit l'`Acteur` de la session et est refusée avant tout appel au dépôt (`AppError.gestionOrganisationReservee`, `capaciteNiveauSuperieurRequise`, `designationResponsablesReservee`, `suppressionNoeudReservee`) ; la validation relit le type du nœud dans le dépôt ; la capacité de `roles.json` est consultée par le contrôleur (`CapacitesController` injecté), et les écrans reprennent la **même** règle pure (`OrganisationAccesRules.peutCreerOuValider`) — aucun seuil dupliqué. Testé en appelant le contrôleur directement, sans écran (`organisation_controller_acces_test.dart`). Première version du lot limitée à l'écran et au routage, resserrée à la demande du porteur du projet (2026-09-24) : un contrôle à l'affichage seul est la faille déjà corrigée aux Modules VIII, X, XXI et II |
| II — Fidèles | ⚠️ partiel — Lot 1 (Domaine & Données) + Lot 2 (Contrôleur & écrans) livrés : `fideles`/`liens_familiaux`/`historique_fideles`/`tuteurs`, RG-II-01/02/03/04/05/06/08 câblés. 7 écrans sur 14 livrés (liste+recherche, fiche+cheminement+liens+tuteur, création, historique, notes pastorales privées). Écrans/règles différés, chacun avec sa dépendance exacte : **écran 8 scan/capture photo** — infra caméra/galerie (ex. `image_picker`), aucun module bloquant ; **écran 9 import rapide contact** — infra carnet d'adresses (ex. `flutter_contacts`), aucun module bloquant ; **écran 10 fiche synthèse imprimable** — infra génération PDF (ex. `printing`/`pdf`), version de base non bloquée, version enrichie RG-II-07 bloquée par III/IV/V/VI/VII/IX/X/XI/XII/XVIII/XIX ; **écran 11 / RG-II-09 fusion de doublons, import de masse** — dans le périmètre propre du Module II, aucun module bloquant ; **écran 12 statuts spirituels (vue par catégorie)** — dans le périmètre propre du Module II (agrégation de `Fidele.statutSpirituel` déjà en base), aucun module bloquant ; **écran 13 notes pastorales privées (RG-II-11, ajoutée au Cahier)** — ✅ livré (2026-09-24), plan validé par le porteur du projet avant code : table dédiée `notes_pastorales` (Drift v23, migration `0026_notes_pastorales.sql`, policies **actives dès la migration**), `noeud_id` figé au nœud du fidèle à la rédaction (la note ne suit pas une mutation) ; lecture par l'auteur ou un pasteur (ou plus, administrateur compris par son rang) du périmètre du nœud de la note, **jamais par le fidèle concerné — exclusion évaluée avant le rang**, même s'il est pasteur de son propre nœud ; rédaction par un pasteur du périmètre en son nom, jamais sur sa propre fiche ; modification du contenu par l'auteur seul, sans historique ; fidèle, auteur et nœud immuables (privilèges de colonne) ; aucune suppression. La liste n'expose que l'auteur et la date ; le contenu n'est servi que par `NotesPastoralesRepository.consulter`, qui vérifie **et** journalise (`consultations_notes_pastorales`, ajout seul, lisible de qui lit la note) — la limite « listes non journalisées » du Module X n'est donc pas reproduite ici. Journalisation = **extension par analogie** de RG-SEC-06 (qui ne l'impose qu'aux dossiers disciplinaires), inscrite comme telle au Cahier. Tous les contrôles sont dans le dépôt avec l'`Acteur` de la session (liste, ouverture, journal, rédaction, modification) ; routes `/fideles/:id/notes-pastorales` et `/notes-pastorales/:id` gardées ; bouton sur la fiche fidèle réservé au rang pasteur, jamais sur sa propre fiche. Tests : règle pure, dépôt (refus, journal, nœud figé après mutation), vraie session (pasteure / responsable / fidèle concerné pasteur, routes directes, aucune trace sur un refus), SQL réel `supabase/tests/0026_notes_pastorales_test.sql` (10 profils + anon, dont contrôle négatif : le gabarit générique `fidele_accessible()` rouvrirait la note au fidèle concerné ; vérifié qu'une policy sans l'exclusion du fidèle concerné fait échouer le test). **Risque connu** : les notes sont stockées en clair dans la base locale de l'appareil tant que RG-SEC-08 (chiffrement au repos) n'est pas construit — même situation que les dossiers disciplinaires. **Droit d'accès légal (RGPD / loi nationale)** : le refus applicatif n'éteint pas le droit d'accès du fidèle — procédure d'export administrateur inscrite au registre §13, hors code ; **droits du Module II (lot « droits I/II/XXIII »)** — ✅ : `FideleAccesRules`, miroir de la policy `fideles` de 0019 — liste et fiches d'autrui au rang responsable (approximation du périmètre, dette RG-SEC-05 ci-dessous), sa propre fiche et son historique en lecture pour tout fidèle enregistré, rien pour un utilisateur simple (RG-SEC-06bis — il ouvrait jusqu'ici la liste complète) ; garde vérifiée **avant** l'existence de la fiche (qui n'est pas révélée), y compris par accès direct à la route ; création, coordonnées, statut spirituel, liens familiaux, tuteur et archivage réservés au rang de gestion **dans le contrôleur** (`FideleController`, `AppError.gestionFidelesReservee`), pas seulement à l'écran. **RG-II-05 enfin tenu** : `FideleRepository` acceptait un `auteurFideleId` qu'aucun écran ne fournissait — l'auteur (fiche de la session) est désormais transmis à chaque historisation et affiché dans l'historique (« Par … · date ») ; serveur (0024) : un auteur autre que la fiche du compte réel est refusé. **Point d'entrée trésorier vers les engagements** — ✅ (2026-09-24) : un trésorier sans mandat (rang membre) n'avait plus de chemin vers l'écran « Engagements » d'un fidèle de son nœud (il passait par la fiche, que la policy `fideles` ne lui ouvre pas). Icône « Engagements des fidèles » sur l'écran Contributions d'un nœud → `EngagementsDuNoeudScreen` (`/organisation/:id/engagements`, noms seuls, mène aux engagements, jamais à la fiche) ; réservé à un pasteur ou au trésorier de **ce** nœud (pas d'un nœud enfant ni parent — même règle que `est_tresorier(noeud_du_fidele(...))`). L'accès aux engagements (`FinancesRules.peutAccederEngagements`) est vérifié **dans le dépôt** pour la lecture (`watchEngagements` émet `AppError.engagementsAccesReserve`), la création et l'honneur d'une échéance, avec l'`Acteur` de la session. La fiche et l'historique du fidèle restent fermés au trésorier, route directe comprise (`finances_acces_test.dart`) ; **écran 14 / RG-II-10 consentement RGPD (écran « Profil et préférences utilisateur », vit dans le Module XXIII)** — le traçage de l'auteur (RG-SEC-01) est désormais disponible (`SessionController.acteur`) ; reste bloqué par RG-SEC-10/Module XVII pour la gestion par canal ; **RG-II-07 agrégation en lecture seule** — bloquée par les modules qu'elle agrège (III, IV, V, VI, VII, IX, X, XI, XII, XVIII, XIX), aucun construit ; **bornage local par périmètre hiérarchique (RG-SEC-05), dette transversale à tous les modules** — aucun écran ne filtre encore localement par nœud du périmètre (arbre du Module I, liste des fidèles, listes des Modules III à XXI) : seuls le rôle et les appartenances nommées (commission, trésorier…) sont appliqués côté client depuis RG-SEC-01 ; le bornage par nœud n'est assuré que côté serveur (policies `0019`, `dans_perimetre()`). Sans risque tant que la base locale ne contient que les données de l'appareil ; **bloquant dès que la synchronisation descendante sera active** (la base locale recevrait alors des lignes hors périmètre si le serveur ne les filtrait pas — il les filtre, mais le client doit aussi appliquer `PerimetreRules`, déjà écrit et testé, pour rester cohérent hors ligne) |
| XXIII — Paramètres de l'application | ⚠️ partiel — RG-XXIII-02 (moteur de capacités `Role`/`CapacityRules`/`roles.json`) et RG-XXIII-03 (référentiel `ZoneGeographique`, désactivation non destructive, **conservé tel quel — décision Kernel tranchée, voir §11 point 1** : zone pastorale interne, distincte de l'adresse Kernel) livrés, avec écrans de consultation (rôles) et de gestion (zones géographiques). Éléments différés, chacun avec sa dépendance exacte : **capacités câblées dans l'UI des Modules I/II** — ✅ après RG-SEC-01 : `roles.json` est chargé **une fois à la racine** (`CapacitesController`, préchargé par `main.dart` avant `runApp`, **refus par défaut** tant qu'il n'est pas chargé ou pour une capacité qu'il ne déclare pas) ; `creer_noeud_niveau_superieur` et `consulter_annuaire_eglises` sont désormais réellement appliquées (elles n'étaient lues que par l'écran de consultation des rôles). Les autres seuils d'accès des Modules I/II restent des **règles pures** (`OrganisationAccesRules`, `FideleAccesRules`) qui reproduisent les policies de 0019 : les rendre « paramétrables » dans `roles.json` alors que le serveur les fige en SQL créerait un écart client/serveur. Tests : la suite précharge `roles.json` une fois (`test/flutter_test_config.dart`, `capacitesDeTest`) et le passe à chaque `EcclesiasApp`, comme `main.dart` — sans quoi les capacités dépendraient du calendrier d'exécution (la lecture d'un asset exige une vraie attente asynchrone) ; **RG-XXIII-01 versionnage propagé des référentiels partagés** — bloqué par l'absence de mécanisme de versionnage dans `SyncCoordinator` (à construire) et par les référentiels don spirituel (Module IV) et type d'offrande (Module XI), non construits — le référentiel zones géographiques existe déjà mais sans versionnage ; **RG-XXIII-04 sauvegardes automatiques** — infra prévue en Phase 6 Durcissement et déploiement (cf. RG-SEC-13), aucun module métier bloquant ; **RG-XXIII-05 multilingue par nœud : non bloqué** — Module I (nœuds) et le socle i18n (ARB) existent déjà, à construire dès que priorisé, indépendamment de la Phase 2 ; **RG-XXIII-06 accès réservé + historisation auteur** — ✅ livré (lot « droits I/II/XXIII », 2026-09-24) : administration des paramètres réservée à l'administrateur (`ParametresRules`) — vérifiée **dans le dépôt** des zones (`AppError.administrationParametresReservee`), garde du routeur (`/parametres/zones-geographiques`, `/parametres/journal`, même précédent que les liaisons de comptes) et entrées masquées dans l'écran Paramètres ; la consultation des rôles (écran mobile 4) reste ouverte en lecture. Toute création, désactivation ou suppression d'une zone est historisée **dans la même transaction** (`journal_parametres`, Drift v21 : auteur = compte et fiche de la session via `SessionController.acteur`, date, instantanés JSON avant/après) ; écran « Journal des modifications » (écran Windows 9 repris en lecture seule sur mobile). Serveur `0024_historisation_parametres_auteur_fideles.sql` : le journal est alimenté par un **déclencheur `security definer` sur les 11 référentiels administrés** (ceux de la boucle « G » de 0019 : zones, types de ministère, dons, compatibilités, professions, groupes, nomenclatures, natures de faute, types d'offrande, catégories de biens, plan comptable), jamais par le client — une écriture qui passe la policy ne peut pas échapper à sa trace, l'auteur est lu dans le jeton ; lecture administrateur seule, ni insertion, ni modification, ni suppression par un client ; testé par `supabase/tests/0024_historisation_parametres_auteur_fideles_test.sql` (échoue bien sans le déclencheur). Le journal local n'est pas destiné à être poussé (le serveur produit le sien). **Écart restant, à traiter avec les Modules III à XXI** : les référentiels autres que les zones sont modifiés localement par les dépôts de leur module, sans contrôle « administrateur » côté client ni journal local — le serveur refuse et historise, mais le client ne le reflète pas encore ; **référentiels dons/offrandes/professions** — dons spirituel bloqué par Module IV, type d'offrande par Module XI, professions par Module V ; **écran « Profil et préférences utilisateur » (RG-II-10)** — RG-SEC-01 livré ; reste bloqué par RG-SEC-10/Module XVII pour la gestion du consentement par canal (voir inventaire du Module II, écran 14) ; d'ici là, un fidèle lit sa propre fiche et son historique en lecture seule |
| III — Ministères et départements | ⚠️ partiel — `Ministere`/`AffectationMinistere`/`MandatResponsable`/`ActiviteMinistere` + catalogue `TypeMinistere` (RG-III-04, 24 types standards seedés, protégés) livrés. RG-III-01 (un seul responsable actif par ministère), RG-III-02 (mandats, échéances), RG-III-05 (historique via mandats clos + journal d'activités) câblés. 7 écrans sur 9 livrés (liste, fiche, création, membres + affectation, historique des responsables, journal d'activités, mandats arrivant à échéance) ; **écran 8 suggestions d'affectation basées sur les dons** — bloqué par le Module IV (Dons spirituels), non construit ; **RG-III-03 suspension disciplinaire** — structure de données prête (`suspendreAffectationsActives`/`reintegrerAffectations`), bloquée par le Module X (Discipline), non construit, non appelée depuis l'UI actuelle. Synchronisation distante limitée à `Ministere` pour cette itération (même limitation documentée que ZonesGeographiques) |
| IV — Dons spirituels | ⚠️ partiel — `DonSpirituel` (référentiel fixe des 9 dons, 1 Corinthiens 12, RG-IV-04, seedé, non modifiable dans cette itération) + `DonFidele` (évaluations append-only, RG-IV-01/02 : l'historique n'est jamais écrasé) + `DonMinistereCompatible` (table de correspondance RG-IV-03, vide par défaut — jugement pastoral/organisationnel hors périmètre technique, alimente la suggestion sans jamais affecter automatiquement) livrés. 6 écrans sur 6 livrés (liste des dons d'un fidèle, évaluation, référentiel, historique d'évolution, suggestions de ministères compatibles, statistiques de répartition par nœud). Synchronisation distante différée pour ce module (même précédent documenté que ZonesGeographiques/Ministere) |
| V — Groupes professionnels | ⚠️ partiel — `Profession` (référentiel hiérarchisé catégorie/métier, RG-V-02, paramétrable, 12 métiers de départ, sans protection « standard » contrairement aux Modules III/IV) + `ProfessionFidele` (déclaration vérifiable, RG-V-01 : `declare` → `verifie`, jamais l'inverse) + `Sollicitation` (RG-V-03, sollicitation nommée tracée avec réponse, une ligne par fidèle sollicité, seules les déclarations vérifiées sont ciblées) livrés. 5 écrans sur 5 livrés (liste des groupes + recherche par métier combinées, fiche compétence d'un fidèle + déclaration combinées, fiche groupe + sollicitation combinées). Synchronisation distante différée pour ce module (même précédent documenté que ZonesGeographiques/Ministere/DonSpirituel) |
| VI — Groupes de l'Église | ⚠️ partiel — `GroupeEglise` (segment démographique/fonctionnel, `typeRegle` auto/manuel) + `AppartenanceGroupe` (non exclusive, RG-VI-02) + `CriteresGroupe` livrés. RG-VI-01 : `GroupeRules.correspond()` évalue un fidèle, `recalculerAppartenancesAuto()` synchronise les lignes auto sans jamais toucher aux lignes manuelles, `affecterManuellement()` applique la priorité à la règle automatique sauf dérogation tracée (motif obligatoire en cas de conflit). 11 groupes de départ (7 auto calculables + 4 manuels sans critère). 4 écrans (liste, membres + affectation manuelle + statistiques combinés, règles automatiques, vue depuis la fiche fidèle). **Recalcul automatique déclenché manuellement** (bouton dédié) — bloqué par l'absence de hook sur la création/modification d'un fidèle (nécessiterait un couplage inter-modules non construit) ; en pratique, un administrateur doit relancer le recalcul après toute modification pertinente de la fiche fidèle. Synchronisation distante différée pour ce module (même précédent documenté que ZonesGeographiques/Ministere/DonSpirituel/Profession) |
| VII — Comité local | ⚠️ partiel — `MembreComite` (RG-VII-01, sous-ensemble qualifié des fidèles) + `QuorumComite` (RG-VII-05, paramétrable par nœud) + `SeanceComite`/présents + `Decision` (RG-VII-04/05) + `ProcesVerbal`/`ErratumPv` (RG-VII-02, immuabilité après validation) + `TacheSuivi` (RG-VII-03) livrés. Le quorum est calculé par le système (jamais déclaré), une décision ne peut être adoptée que si le quorum de sa séance est explicitement atteint. 7 écrans du Cahier couverts par 4 écrans réels (membres+fiche combinés, séances/historique des PV, création de séance combinant convocation+saisie, détail de séance combinant décisions+PV+tâches). **RG-VII-03 archivage automatique (Module VIII)** — ✅ livré : `documentArchiveId` est renseigné automatiquement à la validation du PV quand `ComiteRepository` reçoit un `ArchivageRepository` (voir Module VIII ci-dessus). **RG-VII-04 propagation au Module X** — schéma et API prêts côté Module X (`DossierDisciplinaire.decisionComiteOrigineId`, optionnel, voir ci-dessous) ; `porteeDisciplinaire` reste par ailleurs un simple drapeau informatif sur `Decision`, aucun changement apporté au Module VII lui-même ; le lien entre une décision de comité et un dossier ouvert derrière elle n'est pas encore établi depuis un écran (Lot 2 du Module X, non construit). Synchronisation distante différée pour ce module (même précédent documenté que les modules précédents) |
| XII — Cultes | ⚠️ partiel — `Culte`/`SequenceLiturgique`/`PresenceCulte`/`PublicationCulte`/`PropositionTheme`/`VoteProposition` (RG-XII-01/02/03/05/06) livrés : domaine, données Drift, migration. RG-XII-02 (modes de présence nominal/global mutuellement exclusifs) et RG-XII-05 (séries récurrentes hebdomadaires) câblés dans `CulteRepository`. RG-XII-06 : décompte des votes toujours recalculé depuis `votes_proposition`, jamais un compteur mutable. 9 écrans du Cahier couverts par 4 écrans réels (liste des cultes + création avec option série récurrente, fiche détaillée combinant statut/liturgie/présences/publication post-culte, propositions de thème avec soumission et vote) ; **auteur des soumissions et des votes (RG-XII-06)** — ✅ câblé après RG-SEC-01 : c'est la fiche liée à la session (le sélecteur « voter en tant que » a été retiré ; un compte sans fiche consulte sans agir) ; **correctif à cette occasion** : « les autres fidèles votent » n'était appliqué nulle part — `CulteRules.raisonBlocageVote` interdit désormais à l'auteur de voter sur sa propre proposition (dépôt et écran), avec son miroir serveur `0022_votes_propositions.sql` (testé par `supabase/tests/0022_votes_propositions_test.sql`), qui autorise aussi un fidèle à changer **son propre** vote (comportement local que la policy « ajout seul » de 0019 ne reflétait pas) ; **RG-XII-03 archivage automatique dans la médiathèque (Module XIII)** — ✅ construit (Module XIII, Lot 1) : chaque média non-nul d'une publication post-culte est archivé automatiquement, sans ressaisie manuelle, voir l'entrée Module XIII ; **RG-XII-04 rapprochement des offrandes de culte** — bloqué par le Module XI (Finances), non construit ; **classement automatique des propositions par l'assistant IA (Module XVI, RG-XII-06)** — non construit, `categorie` reste `null`. Synchronisation distante différée pour ce module (même précédent documenté que les modules précédents) |
| VIII — Archivage documentaire | ⚠️ partiel — Lot 1 (domaine, données, migration) livré : `DocumentArchive`/`VersionDocument`/`NomenclatureArchivage` (RG-VIII-01/02/03/04/05). `ArchivageRepository.archiver` attribue un numéro immuable dès l'appel (nomenclature paramétrable, jetons `{type}`/`{noeud}`/`{annee}`/`{sequence}`), crée systématiquement la version 1 ; `nouvelleVersion`/`mettreEnCorbeille`/`restaurerDeCorbeille`/`purgerDefinitivement` couvrent RG-VIII-02/05 (aucune méthode de modification ou de suppression directe n'est exposée — absence d'API délibérée) ; `documentsDe` couvre la navigation croisée RG-VIII-04. Branché en producteur réel : `ComiteRepository` reçoit un `ArchivageRepository?` optionnel et archive automatiquement le PV à sa validation (RG-VII-03), même pattern d'injection optionnelle que CV/Déplacements documenté dans `RECONSTRUCTION_ecclesias360.md` §2. Éléments différés, chacun avec sa dépendance exacte : **RG-VIII-06 (garantie de séquence unique inter-appareils à la synchronisation)** — bloqué par l'absence de synchronisation distante active, même précédent documenté que tous les autres modules ; numérotation locale déterministe pour cette itération ; **écrans (Lot 2)** — ✅ livrés : bibliothèque documentaire avec recherche locale (`DocumentsArchiveListScreen`), consultation d'un document + historique des versions combinés (`DocumentArchiveDetailScreen`, ajout de nouvelle version), corbeille avec purge conditionnée au délai (`CorbeilleDocumentsScreen`) ; point d'entrée depuis la fiche de nœud. Transitions personnalisées (`sharedAxisPage`, glissement + fondu) et apparition échelonnée des listes (`StaggeredFadeIn`) introduites pour ce module, scopées à ses 3 écrans pour l'instant (non retrofittées sur le reste de l'app). Navigation croisée vers l'objet d'origine (RG-VIII-04) limitée à une mention texte (module/identifiant), pas encore cliquable — nécessiterait un registre de routes par module producteur, non construit tant que peu de producteurs existent ; **file d'attente hors ligne, non construite ; **écran « génération depuis un modèle » / éditeur de modèles de documents** — le modèle de données du Cahier ne définit aucune entité `ModeleDocument` (seulement `NomenclatureArchivage`, qui numérote, ne génère pas de contenu), différé faute d'entité porteuse ; **écran « signature/validation »** — reste dans le module producteur (ex. `ComiteRepository.validerProcesVerbal`, déjà existant), Module VIII n'est appelé qu'une fois le contenu définitivement validé ; **restriction d'accès par niveau de confidentialité (RG-VIII-03)** — ✅ appliquée après RG-SEC-01, **règle tirée de la source, pas un masquage générique** : `ArchivageRules.peutConsulterDocument` — un document rattaché à un dossier disciplinaire (origine `discipline`, dont `objetIdOrigine` est l'identifiant du dossier, ou pièce de `pieces_dossier`) n'est lisible que si **chacun** de ces dossiers l'est (`DisciplineRules.peutConsulterDossier` : pasteur ou plus, ou commission assignée), **quel que soit son niveau stocké** ; une origine disciplinaire introuvable est refusée, jamais repliée sur le niveau. Tout autre document suit son niveau : `restreint` à partir du rang pasteur, `standard` à partir du rang responsable (approximation locale du périmètre, dette RG-SEC-05). Chaque `DocumentArchive` porte ses `dossiersRattaches` (recalculés à chaque écriture sur les archives, pièces ou dossiers — via `tableUpdates`, pas une requête observée : Drift ne réémet pas un résultat identique). Vérifiée **dans le dépôt** (`ArchivageRepository.consulter`, `AppError.documentArchiveAccesRefuse`), pas seulement à l'écran ; fiche gardée contre l'accès direct par route, bibliothèque et corbeille filtrées, bouton de la fiche de nœud et écrans réservés au rang responsable ; purge (RG-VIII-05) réservée au pasteur, dans le dépôt et à l'écran. Miroir serveur `0023_confidentialite_archives_journal_discipline.sql` : `document_archive_accessible()` redéfinie selon la même règle — **corrige une faille de 0019** : la policy de modification laissait un pasteur du périmètre abaisser une pièce à `standard`, la rendant lisible par tout responsable du périmètre hors de la règle du dossier ; niveau, origine, objet d'origine et nœud désormais immuables (privilèges de colonne, seules `fichier`/`statut`/`date_mise_corbeille`/`updated_at` restent modifiables) ; un rattachement de pièce forgé ne peut que restreindre, jamais ouvrir. Testé par `supabase/tests/0023_confidentialite_archives_journal_discipline_test.sql` (4 profils ; échoue bien avec l'ancienne fonction de 0019 réinjectée) ; jeu de données du test 0019 corrigé (ses pièces avaient `objet_id_origine = 'x'`, état que l'application ne produit jamais). **Journal des consultations (RG-SEC-06)** : toute lecture d'une pièce disciplinaire archivée est journalisée, voir Module X. Synchronisation distante différée pour ce module (même précédent documenté que les modules précédents) |
| IX — Déplacements | ⚠️ partiel — Lot 1 (domaine, données, migration) livré : `Mutation`/`LettreRecommandation` (RG-IX-01/02/03/04). `DeplacementRepository.demanderMutation` crée la mutation `enAttente` (RG-IX-04 : aucune statistique touchée avant validation) ; `validerCote` porte la double validation pastorale (`valideeParOrigine`/`valideeParDestination`, champs ajoutés au-delà du tableau minimal du Cahier), avec politique de validation unilatérale paramétrable (`AppDefaults.deplacementValidationUnilateraleAutorisee`, `false` par défaut, RG-IX-01) ; une fois validée, le rattachement du fidèle change via la nouvelle méthode `FideleRepository.changerNoeud` (historisée dans `historique_fideles`, RG-IX-03) et la lettre de recommandation est archivée dans le Module VIII (RG-IX-02, `ArchivageRepository?` optionnel, échec non bloquant — même pattern que `ComiteRepository`) ; `refuserMutation`, `mutationsDe`/`watchMutations` couvrent le reste. Nom `DeplacementRepository` et méthode `validerCote` repris de `RECONSTRUCTION_ecclesias360.md` §2 pour rester compatibles avec le futur point d'intégration CV Ecclésiastique (`annexerCvAuTitulaire`, non câblé — l'extension CV n'existe pas encore, voir AGENTS.md §11). Écrans (Lot 2) — ✅ livrés : liste des mutations d'un nœud avec demande (`MutationsListScreen`, FAB « Demander une mutation », dialogue fidèle/destination/motif), fiche de mutation combinant les deux validations pastorales et le lien vers la lettre archivée (`MutationDetailScreen`), historique des déplacements d'un fidèle (même écran `MutationsListScreen`, lecture seule) ; points d'entrée depuis la fiche de nœud et la fiche de fidèle. Reprend les transitions (`sharedAxisPage`) et l'apparition échelonnée (`StaggeredFadeIn`) introduites au Module VIII. Synchronisation distante différée pour ce module (même précédent documenté que les modules précédents) |
| X — Discipline | ⚠️ partiel — Lot 1 (domaine, données, migration) livré : `NatureFaute` (référentiel fermé et extensible, RG-X-02) + `CommissionDisciplinaire`/`MembreCommission` + `DossierDisciplinaire` (RG-X-01/02/03/04) + `PieceDossier` (RG-X-02/06). **Référentiel `NatureFaute` — contrainte permanente, à respecter dans tous les lots suivants et toute évolution de la liste : strictement administratif/procédural (gouvernance, organisation, engagements — ex. absentéisme prolongé, désobéissance à l'autorité pastorale, conflit non résolu, manquement à un engagement/mandat). N'y inscrire, sous aucun prétexte ni à la demande d'aucun module ou utilisateur, de catégorie à caractère moral, doctrinal ou théologique (ex. infidélité conjugale, doctrine déviante, péché sexuel) — un tel jugement reste une décision pastorale rendue hors du logiciel, jamais une taxonomie codée en dur. La table reste éditable/extensible (4 natures seedées pour amorcer, `standard: true`), mais cette limite de contenu n'est, elle, pas négociable.** `DisciplineRepository.ouvrirDossier` (RG-X-01) bascule le statut spirituel du fidèle (module II) vers `membreEnDiscipline` via `FideleRepository.modifierStatutSpirituel(viaModuleDiscipline: true)`, habilité soit par rang de rôle (`CapacityRules`/`Role`, module XXIII, ≥ pasteur), soit par appartenance de l'acteur à une commission du nœud — capacité déjà appliquée au niveau du dépôt (pas seulement de l'UI), car `CapacityRules` est pure et ne dépend pas de RG-SEC-01. **Correctif Module II à l'occasion de ce lot** : `FideleRules.validerTransitionStatutSpirituel` n'autorisait l'entrée en `membreEnDiscipline` via `viaModuleDiscipline` qu'à sens unique — la sortie retombait dans le contrôle de progression en chaîne et échouait toujours ; ajout d'une branche symétrique (avec test) rendant RG-X-04 réellement fonctionnel. `prononcerDecision` (RG-X-02, exige une commission assignée) calcule `dateReintegrationPrevue` si la durée est déterminée (`DisciplineRules.calculerDateReintegrationPrevue`) et suspend les affectations ministérielles actives (module III, `MinistereRepository.suspendreAffectationsActives`, déjà prêtes avant ce lot) si demandé ; `cloturer` (RG-X-04) restaure le statut spirituel antérieur exact (capturé à l'ouverture, `statutSpirituelAnterieur`) et réintègre les affectations si elles avaient été suspendues. `dossiersEnAlerteFinDePeriode`/`dossiersEnRevuePeriodique` (RG-X-04, seuil `AppDefaults.disciplineRevuePeriodiqueJours` = 90 jours) fournissent la lecture nécessaire aux futurs écrans d'alerte, sans scheduler ni notification poussée (aucune infra de ce type dans le dépôt). RG-X-06 : `ajouterPiece` archive dans le Module VIII avec confidentialité `restreint` (le niveau maximal disponible aujourd'hui) quand un `ArchivageRepository` est injecté, même précédent d'injection optionnelle que Comité/Déplacements (nomenclature `piece_dossier_disciplinaire` seedée localement et côté Supabase) ; limité aux pièces textuelles (témoignage écrit) faute d'infra caméra/galerie, même limite que documentée pour le Module II. Écrans (Lot 2) — ✅ livrés : les 6 écrans mobile du Cahier couverts par 2 écrans réels (liste des dossiers d'un nœud + ouverture combinées, et le même écran réemployé pour l'historique confidentiel d'un fidèle — même motif que `MutationsListScreen` du Module IX ; fiche de dossier combinant commission/instruction, pièces, décision et sanction, suivi de réintégration). L'acteur d'une ouverture est la session (rôle et fiche liée du `SessionController`) — les anciens sélecteurs « rôle agissant » / « ouvert par » ont été retirés une fois RG-SEC-01 livré. Alertes de fin de période/revue périodique affichées en badge sur la liste et la fiche plutôt qu'en écran Windows dédié (même précédent que les autres modules partiels). Reprend les transitions (`sharedAxisPage`) et l'apparition échelonnée (`StaggeredFadeIn`) introduites au Module VIII ; aucun écran de la maquette `ecclesias360_Nouveau` ne couvre la discipline, style général repris par cohérence. **Point d'attention retenu pour les futurs écrans à listes dynamiques (pièces, membres, etc.) accumulées sur un même écran** : un `ListView` simple ne construit que les éléments dans la fenêtre visible (+ cache) — un bouton ou champ ajouté plus bas dans une fiche qui grandit (ex. après ajout d'une pièce) peut ne pas être immédiatement atteignable par `ensureVisible` (qui exige l'élément déjà construit) ; utiliser `dragUntilVisible`/`scrollUntilVisible` à la place, comme déjà pratiqué pour les tuiles du tableau de bord et l'écran Compétences professionnelles. De même, un `await` sur un flux Drift (`.first`) exécuté juste avant l'ouverture d'un dialogue (`showDialog`) s'est avéré ne pas se résoudre de façon fiable dans la fenêtre de `pumpAndSettle()` en test ; préférer toujours des données déjà disponibles de façon synchrone (ex. issues d'un `StreamBuilder` englobant déjà construit) plutôt qu'un nouvel `await` au moment du clic. Confirmé une seconde fois au Module XI (Finances, Lot 2) : `EngagementsListScreen._honorer` appelait `await controller.watchTypesOffrande().first` directement dans le gestionnaire du bouton « Honorer », provoquant un blocage silencieux du test de flux (l'échéance ne passait jamais à « Honorée ») — corrigé en enveloppant l'écran entier d'un `StreamBuilder<List<TypeOffrande>>` et en passant `typeOffrandeId` déjà résolu en paramètre. Ce motif (`await` sur `.watch().first`) doit être considéré comme systématiquement à proscrire dans un gestionnaire d'événement, dialogue ou non. Éléments différés, chacun avec sa dépendance exacte : **RG-X-05 (restriction d'accès réelle par rôle)** — ✅ câblé après RG-SEC-01 : `DisciplineRules.peutConsulterDossier` (miroir local de la policy `dossiers_disciplinaires_lecture` de 0019) — un dossier n'est visible que d'un pasteur (ou plus) ou d'un membre de la commission assignée, jamais du fidèle concerné à ce seul titre ; liste filtrée, fiche gardée contre l'accès direct par route, boutons d'accès (fiche de nœud, fiche de fidèle) masqués sans habilitation (`AccesDisciplineBuilder`). Le filtrage local ne borne pas encore par périmètre hiérarchique (RG-SEC-05) — dette transversale inscrite dans l'inventaire du Module II ; **livraison effective des alertes de fin de période/revue périodique** (au-delà du badge) — bloquée par l'absence de tout module de notification (XVII, non construit), seules les méthodes de requête existent. **Journal systématique des consultations (RG-SEC-06)** — ✅ livré (Drift v20, table `consultations_disciplinaires`, ajout seul) : `JournalConsultationsRepository` (`lib/core/audit/`, socle local en attendant l'Audit du Kernel, Cahier §8.5, partagé par X et VIII sans dépendance entre eux) ; `DisciplineRepository.consulterDossier` vérifie l'habilitation **dans le dépôt** (`AppError.dossierDisciplinaireAccesRefuse`) puis journalise chaque ouverture de fiche ; `ArchivageRepository.consulter` journalise chaque lecture d'une pièce archivée (une ligne par dossier rattaché, `documentArchiveId` renseigné). Trace : compte (`authUserId`), fiche liée (nulle pour un compte sans fiche), rôle, date. Les deux fiches (dossier, document) lancent la consultation une seule fois (`initState`) : une reconstruction d'écran n'est pas une nouvelle consultation. Section « Journal des consultations » sur la fiche du dossier, visible d'un pasteur ou plus (le journal révèle qui s'est intéressé à un dossier : pas ouvert à la commission). Serveur (0023) : lecture pasteur du périmètre ; insertion seulement de sa propre consultation (compte, fiche et rôle réels, dossier/pièce effectivement lisibles, pas de date future) ; ni UPDATE ni DELETE ; aucun accès `anon`. **Limites connues, à ne pas perdre** : (1) les **listes** de dossiers (d'un nœud, historique d'un fidèle) ne sont pas journalisées — seule l'ouverture d'une fiche ou d'une pièce l'est ; (2) le journal est écrit localement et, comme toutes les tables, n'est **pas encore synchronisé** vers le serveur ; (3) une lecture faite directement contre l'API (PostgREST, jeton d'un utilisateur habilité) n'est pas journalisée — PostgreSQL n'a pas de déclencheur sur SELECT ; la fermer exigera de servir ces lectures par une fonction `security definer` qui journalise, à concevoir avec la synchronisation descendante ; (4) la policy d'insertion exige le rôle **actuel** : une consultation faite hors ligne puis synchronisée après un changement de rôle sera refusée (trace intègre plutôt que falsifiable, à traiter dans l'outbox le moment venu). Synchronisation distante différée pour ce module (même précédent documenté que les modules précédents) |
| XI — Finances | ⚠️ partiel — Lot 1 (domaine, données, migration) livré : `TypeOffrande` (référentiel fermé et extensible, RG-XI-01 — porté par le Module XI lui-même, même précédent que `TypeMinistere`/`DonSpirituel`/`Profession`, malgré le rattachement conceptuel du Cahier au Module XXIII, voir §7 entrée XXIII) + `TresorierNoeud` (RG-XI-02 — table latérale habilitant la validation comptable, l'énum `Role` du Module XXIII étant fermée et sans valeur « trésorier », même motif que `MembreCommission`/`MembresComite`, décision validée par l'utilisateur) + `Contribution` (RG-XI-01/02/05/06) + `Projet`/`DepenseProjet` (RG-XI-03) + `Engagement`/`EcheanceEngagement` (RG-XI-04). Devise : simple `String` (`AppDefaults.financesDeviseParDefaut`, 'GNF') plutôt qu'une colonne Kernel `devise_id` — la « GSG Referential » (devise) n'existe pas encore et §11 point 2 a déjà tranché que ces colonnes Kernel n'entrent qu'avec leur vrai module consommateur (facturation/abonnement), pas avant, décision validée par l'utilisateur. `FinancesRepository.saisirContribution` (RG-XI-01) démarre toujours `enAttente` ; `validerContribution` (RG-XI-02) exige soit un rang de rôle ≥ pasteur (`CapacityRules`/`Role`), soit la désignation trésorier du nœud de la contribution (`estTresorierDuNoeud`, vérifiée sur l'acteur validant — `valideParFideleId` —, jamais sur le donateur), et trace l'auteur (`valideParFideleId`) ; `rejeterContribution` couvre l'autre issue. `contrePasserContribution` (RG-XI-05) : une contribution validée n'est jamais éditée — la correction crée une nouvelle contribution de montant inverse, directement validée, qui référence l'originale (`contributionOrigineId`/`estContrePassation`), même convention d'immuabilité après validation que `ProcesVerbal` (Module VII). `detecterDoublonsPotentiels` (RG-XI-06) compare fidèle/montant/minute parmi les contributions déjà saisies au même nœud, pour rapprochement à la reconnexion (aucune fusion/rejet automatique). `soldeProjet` (RG-XI-03) n'est jamais une colonne stockée : recalculé à la lecture (recettes validées rattachées moins dépenses), même précédent que le décompte des votes (RG-XII-06) ; `ajouterDepenseProjet` bloque toute dépense dépassant le solde disponible sauf `derogationTracee` explicite (motif tracé). `creerEngagement` (RG-XI-04) génère immédiatement ses échéances (`en_attente`, périodicité paramétrable) ; `echeancesEnRetard`/`honorerEcheance` couvrent le suivi, sans scheduler ni notification poussée (aucune infra de ce type dans le dépôt, même limite que les alertes du Module X). Migration `supabase/migrations/0014_module_xi_finances.sql` livrée (RLS activée, sans `create policy`, même précédent que les modules précédents) et tests unitaires/dépôt couvrant chaque règle ci-dessus. Écrans (Lot 2) — ✅ livrés : les 8 écrans mobile du Cahier couverts par 6 écrans réels — « Contributions » (`ContributionsListScreen`, noeudId/fideleId au même motif xor que `MutationsListScreen`/`DossiersDisciplinairesListScreen`) combine la saisie rapide d'offrande (FAB), l'historique d'un fidèle et le rapport rapide par type d'offrande (section récapitulative calculée côté client à partir des contributions validées déjà chargées, sans écran séparé) ; « Reçu de contribution » (`ContributionDetailScreen`) sert aussi d'écran d'action pour la validation, le rejet et la contre-passation (RG-XI-05) — l'acteur est la session (rôle et fiche liée, tracée dans `valideParFideleId`), les anciens sélecteurs « rôle agissant » / « validé par » ont été retirés une fois RG-SEC-01 livré ; un compte habilité mais sans fiche ne peut pas décider (message l'invitant à « Lier mon compte à cette fiche », 0020) ; **correctif à cette occasion** : le rejet et la contre-passation n'exigeaient aucune habilitation et la contre-passation ne traçait pas son valideur — ils exigent désormais la même habilitation que la validation (RG-XI-02) et tracent leur auteur (le rejet dans `valideParFideleId`, auteur de la décision comptable, `dateValidation` restant vide) ; « Trésoriers désignés » (`TresoriersNoeudScreen`, RG-XI-02) reprend le motif de `MembresComiteScreen`, accessible depuis une icône de la barre d'app de l'écran Contributions plutôt qu'un bouton supplémentaire sur la fiche de nœud ; « Liste des projets »/« Fiche projet » (`ProjetsListScreen`/`ProjetDetailScreen`, RG-XI-03) affichent le solde recalculé à chaque émission du flux des dépenses (jamais une valeur figée à l'ouverture) et permettent d'engager une dépense avec dérogation tracée ; « Engagements et échéances » (`EngagementsListScreen`, RG-XI-04) crée un engagement et honore une échéance en saisissant dans la foulée la contribution correspondante (liée, pas automatiquement validée — la validation comptable reste un acte distinct, RG-XI-02). Les écrans fiche/liste utilisent le motif `FutureBuilder` + `StatefulWidget` + `_rafraichir()` déjà en place pour Discipline (une entité unique, contrairement à une liste, n'a pas de flux dédié par identifiant). « File de synchronisation hors ligne » et « Rapport rapide par type d'offrande » en écran séparé — non repris (le second est absorbé dans l'écran Contributions, le premier reste une infra transverse jamais construite pour aucun module, même précédent que Module VIII). Points d'entrée depuis la fiche de nœud (Contributions, Projets) et la fiche de fidèle (Contributions, Engagements). Reprend les transitions (`sharedAxisPage`) et l'apparition échelonnée (`StaggeredFadeIn`) introduites au Module VIII. **Limite connue** : le solde affiché sur une carte de `ProjetsListScreen` (liste) n'est recalculé qu'à l'ouverture de cet écran, pas en direct pendant qu'on y reste (contrairement à la fiche détaillée, qui, elle, se recalcule à chaque émission du flux des dépenses) — cosmétique, sans incidence sur RG-XI-03 (la fiche fait foi). Éléments différés, chacun avec sa dépendance exacte : **RG-XI-07 (consolidation/visualisation)** — la génération automatique des écritures miroir existe désormais (Module XXI, Lot 1), mais l'écran de consolidation/visualisation reste bloqué par le Module XXII (Tableau de bord), non construit ; **relance/notification des échéances en retard** — bloquée par l'absence du Module XVII (Communication), même précédent documenté pour les alertes du Module X ; **restriction d'accès réelle par rôle** — ✅ câblée après RG-SEC-01 : `FinancesRules.peutConsulterContribution` (miroir de la policy `contributions_lecture` de 0019 — pasteur ou plus, trésorier du nœud, ou le donateur) et `AccesFinances` (projets : rang responsable ou trésorier ; historique d'un fidèle : lui-même, pasteur ou trésorier de son nœud ; désignation des trésoriers : pasteur ou plus) appliqués aux listes, aux fiches (accès direct par route compris) et aux points d'entrée ; **séparation stricte saisie/validation (RG-XI-02, RG-SEC-06)** — ✅ livrée (option A tranchée par le porteur du projet) : chaque saisie trace son auteur (`saisie_par_fidele_id`, fiche liée à la session — un compte sans fiche ne saisit pas ; Drift v19, migration `0021_separation_saisie_validation.sql` testée par `supabase/tests/0021_separation_saisie_validation_test.sql`) et `FinancesRules.raisonBlocageSeparationTaches` interdit à cet auteur de valider **ou** de rejeter sa propre saisie, **administrateur compris** ; côté serveur, la policy de décision l'interdit aussi, impose que le décideur tracé soit le compte réel, et des privilèges de colonne empêchent une décision de réécrire le montant, le donateur ou l'auteur de la saisie. Une contre-passation (acte comptable unique, RG-XI-05) a pour auteur son valideur. Une saisie antérieure à v19 n'a pas d'auteur connu à opposer. **Conséquence assumée, pas une dette** : un site ne comptant qu'une seule personne habilitée voit ses propres saisies rester « en attente » jusqu'à ce qu'une seconde personne habilitée (pasteur ou trésorier du nœud) les valide — c'est exactement le cas que RG-XI-02 existe pour neutraliser (une seule personne qui saisit et valide = absence de contrôle). Ce blocage est le comportement correct ; il ne doit **jamais** être « corrigé » par une exception administrateur, un mode mono-utilisateur ou une auto-validation, même à la demande d'un déploiement. L'écran l'explique à l'auteur de la saisie au lieu de lui proposer les boutons de décision. Synchronisation distante différée pour ce module (même précédent documenté que les modules précédents) |
| XX — Biens (patrimoine) | ⚠️ partiel — Lot 1 (domaine, données, migration) livré : `CategorieBien` (référentiel fermé et extensible, RG-XX-01, neuf catégories seedées — terrains, bâtiments, véhicules, instruments de musique, caméras, sonorisation, ordinateurs, mobilier, stocks — mêmes précédent que `TypeOffrande`/`NatureFaute`) + `Bien` (RG-XX-01/02/05, `idInventaire` unique distinct de l'identifiant technique) + `ReservationBien` (RG-XX-03) + `MouvementStock` (RG-XX-05) + `CampagneInventaire`/`PointageInventaire` (RG-XX-04, entités ajoutées au-delà du tableau minimal du Cahier, même précédent que `ErratumPv` du Module VII : le Cahier décrit la règle de recensement périodique sans nommer l'entité porteuse) livrés. `PatrimoineRepository.sortirBien` (RG-XX-02) exige un rang de rôle ≥ pasteur (`CapacityRules`/`Role`, même seuil que la validation comptable du Module XI) et bloque la sortie d'un bien déjà cédé/donné/mis au rebut ; la sortie reste tracée localement sur `Bien` (`typeSortie`/`dateSortie`/`valideParFideleIdSortie`/`motifSortie`) et, depuis le Module XXI (Comptabilité), génère aussi automatiquement son écriture miroir (débit charges de sortie/crédit immobilisations, RG-XX-02/RG-XXI-02) via un `ComptabiliteRepository?` injecté optionnellement, même motif d'injection qu'Archivage/Comité. `reserverBien` (RG-XX-03) empêche tout conflit de double réservation sur la même plage horaire pour un même bien (`PatrimoineRules.seChevauchent`) ; `culteId` référence un culte existant (Module XII) et `objetLibre` porte la description libre pour un événement (Module XVIII, non construit) ou un autre usage. `quantiteStock`/`biensSousSeuilAlerte` (RG-XX-05) ne stockent jamais la quantité en stock, recalculée à la lecture (entrées moins sorties), même précédent que le solde de projet (Module XI, RG-XI-03) ; le seuil d'alerte est un champ optionnel du bien lui-même (`seuilAlerteStock`), et non une configuration séparée par nœud — simplification délibérée, le Cahier ne détaillant pas cette granularité. `demarrerCampagne`/`enregistrerPointage`/`cloturerCampagne` (RG-XX-04) comparent l'état (et, pour un bien à gestion de stock, la quantité) déclaré au système au constat terrain et figent un `ecartDetecte` au moment du pointage (enregistrement d'audit, à la différence d'un solde courant) ; aucun pointage n'est possible sur une campagne déjà clôturée. Le signalement d'état/panne (écran mobile 4) réutilise directement le champ `etat` du bien (`signalerEtat`), le Cahier ne définissant aucune entité porteuse pour un historique de signalement dédié. Migration `supabase/migrations/0015_module_xx_patrimoine.sql` livrée (RLS activée, sans `create policy`, même précédent que les modules précédents) et tests unitaires/dépôt couvrant chaque règle ci-dessus. Écrans (Lot 2) — ✅ livrés : les 6 écrans mobile du Cahier couverts par 4 écrans réels — « Inventaire des biens » (`BiensListScreen`) combine la liste, la création (avec seuil d'alerte de stock conditionnel à la catégorie « stocks ») et la section « Stocks et seuils d'alerte » (RG-XX-05, `biensSousSeuilAlerte` recalculé à chaque émission de la liste des biens elle-même, même limite cosmétique documentée que le solde de `ProjetsListScreen` du Module XI) ; l'accès aux campagnes d'inventaire se fait par une icône de la barre d'app, même motif que l'accès aux trésoriers depuis l'écran Contributions du Module XI ; « Fiche du bien » (`BienDetailScreen`) sert aussi d'écran d'action pour le signalement d'état/panne, la sortie du patrimoine (RG-XX-02, acteur tiré de la session), la réservation (RG-XX-03, bascule vers un champ libre quand aucun culte n'existe pour le nœud) et, pour un bien à gestion de stock, les mouvements de stock (quantité recalculée à chaque émission des mouvements, motif `StreamBuilder` englobant une `FutureBuilder` reconstruite à chaque émission, comme le solde de projet du Module XI) ; « Campagnes d'inventaire » (`CampagnesInventaireScreen`) liste les campagnes et démarre une nouvelle campagne ; « Fiche de campagne » (`CampagneDetailScreen`) liste les pointages et permet la clôture, la liste des biens du nœud nécessaire au choix du bien pointé provenant d'un `StreamBuilder` englobant plutôt que d'un `await stream.first` dans le gestionnaire du bouton — piège déjà documenté aux Modules X et XI. Point d'entrée depuis la fiche de nœud (bouton « Biens »). Reprend les transitions (`sharedAxisPage`) et l'apparition échelonnée (`StaggeredFadeIn`) introduites au Module VIII. Test de flux bout-en-bout (`patrimoine_flow_test.dart`) couvrant : ajout d'un bien, signalement d'état, réservation, sortie, ajout d'un bien à gestion de stock, mouvement de stock avec alerte de seuil, démarrage d'une campagne, pointage avec écart détecté et clôture. Éléments différés, chacun avec sa dépendance exacte : **mobilisation croisée avec le Module XVIII (Événements)** — non construit, seul `objetLibre` (texte libre) couvre ce cas pour l'instant ; **restriction d'accès réelle par rôle** — ✅ câblée après RG-SEC-01 : accès au patrimoine d'un nœud (liste des biens, fiche, campagnes d'inventaire, bouton de la fiche de nœud) réservé au rang responsable ou plus (`peutGererBiens`, approximation locale de la policy `biens_acces` de 0019 — périmètre —, en attendant le bornage local par périmètre, dette RG-SEC-05 de l'inventaire du Module II) ; sortie d'un bien (RG-XX-02) réservée à un pasteur ou plus, l'acteur étant la session (rôle et fiche liée, tracée dans `valideParFideleIdSortie`) — les anciens sélecteurs « rôle agissant » / « validé par » ont été retirés ; un compte habilité sans fiche est invité à « Lier mon compte à cette fiche » (0020). Synchronisation distante différée pour ce module (même précédent documenté que les modules précédents) |
| XXI — Comptabilité | ⚠️ partiel — Lot 1 (domaine, données, migration) livré : `CompteComptable` (plan comptable paramétrable, référentiel générique — le Cahier n'impose aucun plan comptable réel — 7 comptes seedés : Banque, Caisse, Immobilisations et biens, Produits des contributions, Produits de cession de biens, Charges diverses, Charges — sorties d'actifs, RG-XXI-01) + `PeriodeComptable` (exercice, entité globale sans `noeudId`, le Cahier ne le prévoit pas, RG-XXI-03) + `EcritureComptable` (RG-XXI-01/02/03/05, `rapproche` ajouté au-delà du tableau minimal du Cahier pour le rapprochement bancaire, même précédent que `Bien.seuilAlerteStock` du Module XX) + `Budget` (RG-XXI-04, `seuilAlertePourcentage` ajouté au même titre). Toute opération comptable génère exactement deux lignes équilibrées (partie double, débit + crédit) partageant le même `pieceJustificativeId` via `ComptabiliteRepository._enregistrerLignesDoublePartie`, jamais une écriture à ligne unique ; bloque toute écriture sur une période déjà clôturée (RG-XXI-03, immuabilité — la correction se fait par une nouvelle écriture dans la période courante, jamais par réouverture) ; `cloturerPeriode` exige un rang de rôle ≥ pasteur (`CapacityRules`/`Role`, même seuil que la validation comptable du Module XI). Branché en **consommateur** des Modules XI et XX via le même motif d'injection optionnelle qu'`ArchivageRepository`→Comité/Déplacements/Discipline : `FinancesRepository` et `PatrimoineRepository` reçoivent chacun un `ComptabiliteRepository?` optionnel (une seule instance partagée, câblée dans `app.dart`) et génèrent automatiquement leur écriture miroir après leur propre écriture — `validerContribution`/`contrePasserContribution` (RG-XI-02/05, débit caisse/crédit produits, comptes échangés pour la contre-passation via un montant signé) et `sortirBien` (RG-XX-02, débit charges de sortie/crédit immobilisations) — sans aucune ressaisie manuelle, conformément à RG-XXI-02. `soldeCompte` (RG-XXI-01/06) recalcule toujours le solde à la lecture depuis les écritures (jamais un compteur mutable, même précédent que le solde de projet du Module XI/la quantité de stock du Module XX) et sait consolider un nœud et tous ses descendants via le chemin matérialisé `path` (`organisation_nodes.path LIKE '$path%'`, même motif que `OrganisationNodeRepository.deplacerNoeud`), sans jamais de double comptage puisqu'une écriture n'est imputée qu'à un seul nœud. `budgetDepasse` (RG-XXI-04) et `ecartRapprochement`/`marquerRapprochees` (RG-XXI-05) complètent le lot. Migration `supabase/migrations/0016_module_xxi_comptabilite.sql` livrée (RLS activée, sans `create policy`, même précédent que les modules précédents) et tests unitaires/dépôt couvrant chaque règle ci-dessus, y compris les interactions producteur avec `FinancesRepository`/`PatrimoineRepository`. **Portée mobile intentionnellement réduite à la consultation** : 3 des 4 écrans mobile du Cahier (consultation de caisse, journal des écritures récentes, rapport financier rapide) sont tous en lecture seule — aucune saisie manuelle d'écriture n'existe sur mobile, conformément à RG-XXI-02 (« sans ressaisie manuelle ») : toutes les écritures proviennent exclusivement des Modules XI et XX. Écrans (Lot 2) — ✅ livrés : les 3 écrans combinés en un seul écran à onglets (`ComptabiliteScreen` — Caisse/Écritures/Rapport), même motif de combinaison qu'un écran à plusieurs vues d'autres modules (ex. Contributions du Module XI) ; point d'entrée depuis la fiche de nœud (bouton « Comptabilité »). Test de flux bout-en-bout (`comptabilite_flow_test.dart`) couvrant : saisie et validation d'une offrande (Module XI), puis consultation du solde de caisse mis à jour, du journal des deux écritures équilibrées générées et du rapport financier rapide (recettes/dépenses/solde net). **4e écran mobile (« Justificatifs numérisés ») non construit** : `piece_justificative_id` n'est qu'un simple identifiant texte, aucune infra de numérisation/capture n'existe dans le dépôt — même limite documentée que l'écran 8 du Module II et les pièces du Module X. La gestion manuelle du plan comptable, des périodes/clôtures, des budgets et du rapprochement bancaire est listée par le Cahier comme des écrans Windows uniquement — **premier cas où cette limite documentée s'applique au CRUD complet d'une entité, pas seulement à une mise en page multi-colonnes** (précédent similaire au Module I, mais plus large) ; le domaine/dépôt les couvre déjà intégralement (exercés par les tests), sans écran mobile dédié. Éléments différés, chacun avec sa dépendance exacte : **écran « Justificatifs numérisés »** — bloqué par l'absence d'infra caméra/galerie, même précédent que l'écran 8 du Module II ; **RG-XI-07/consolidation multi-nœuds pour un futur tableau de bord** — la brique (`soldeCompte(inclureDescendants: true)`) existe déjà, l'écran consolidé reste bloqué par le Module XXII (Tableau de bord), non construit ; **restriction d'accès réelle par rôle** — ✅ câblée après RG-SEC-01 : `ComptabiliteRules.peutConsulterComptabilite` (miroir de la policy `ecritures_comptables_lecture` de 0019 — pasteur ou plus, ou trésorier désigné du nœud quel que soit son rang), exposée par `AccesFinances.peutConsulterComptabilite` (l'ensemble des nœuds du trésorier y est déjà tiré de la session) ; bouton de la fiche de nœud masqué et écran gardé contre l'accès direct par route. Le bornage par périmètre hiérarchique reste la dette RG-SEC-05 de l'inventaire du Module II. Aucun sélecteur « rôle agissant » à retirer : aucun écran mobile ne mute la comptabilité (`cloturerPeriode` n'a pas d'écran, seuil pasteur identique côté serveur). Synchronisation distante différée pour ce module (même précédent documenté que les modules précédents) |
| XIII — Médiathèque chrétienne | ⚠️ partiel — Lot 1 (domaine, données, migration) livré : `ContenuMediatheque` (RG-XIII-01/02, `TypeContenuMediatheque` fermé — audio/vidéo/podcast/ebook/magazine/document) + `Favori` (RG-XIII-04, unique par couple fidèle/contenu) + `Commentaire` (RG-XIII-03). **Décision explicite de scope, demandée à l'utilisateur avant ce lot (le module manipule pour la première fois du contenu média réel, contrairement aux modules précédents) : « métadonnées seulement ».** `ContenuMediatheque.fichier` n'est qu'une simple référence texte (URL) ; aucun téléversement de fichier réel, aucune lecture audio/vidéo intégrée, aucun téléchargement hors ligne ni gestion de l'espace de stockage local ne sont construits dans cette itération — `motsCles` (`List<String>`) est stocké en texte séparé par des virgules (pas de type tableau natif en sqlite/Drift), même simplification que documentée pour d'autres champs répétés du projet. `MediathequeRepository.watchCatalogue` (écran « Catalogue » du Cahier) ne retourne que les contenus publiés ; `rechercherParTheme` (RG-XIII-01) filtre en plus par thème. `ajouterCommentaire` (RG-XIII-03) part du statut initial calculé par `MediathequeRules.statutInitialCommentaire` selon que le contenu impose une modération a priori ou a posteriori ; `signalerCommentaire` masque automatiquement un commentaire publié au-delà d'un seuil de signalements paramétrable (`AppDefaults.mediathequeSeuilSignalementsAvantMasquage` = 3, `MediathequeRules.doitMasquerAutomatiquement`) ; `modererCommentaire` couvre la décision explicite de modération. `toggleFavori` (RG-XIII-04) bascule l'état favori d'un contenu pour un fidèle. Branché en **producteur consommé** par le Module XII (Cultes) via le même motif d'injection optionnelle qu'`ArchivageRepository`/`ComptabiliteRepository` dans les modules précédents : `CulteRepository` reçoit un `MediathequeRepository?` optionnel et, à chaque `publier` d'une publication post-culte (RG-XII-03), archive automatiquement chaque média non-nul (audio/vidéo/document PDF) comme un contenu déjà publié dans la médiathèque, sans ressaisie manuelle (RG-XIII-02) — idempotent via la contrainte `UNIQUE (source_module, source_id, type_contenu)` sur `ContenusMediatheque` : republier le même culte met à jour la référence de fichier existante plutôt que d'en dupliquer une nouvelle. Migration `supabase/migrations/0017_module_xiii_mediatheque.sql` livrée (RLS activée, sans `create policy`, même précédent que les modules précédents ; aucun seed référentiel, contrairement aux modules avec une liste fermée de types comme TypeMinistere/TypeOffrande/ComptesComptables — le Cahier ne prévoit ici aucune liste standard) et tests unitaires/dépôt couvrant chaque règle ci-dessus, y compris l'interaction producteur avec `CulteRepository` (archivage, absence d'archivage sans média, idempotence à la republication, comportement inchangé sans injection). Écrans (Lot 2) — ✅ livrés : les 11 écrans mobile du Cahier couverts par 3 écrans réels — « Catalogue médiathèque » (`MediathequeCatalogueScreen`, écrans 1+6 combinés) affiche le catalogue publié (`watchCatalogue`) ou, un mot-clé saisi dans le champ de recherche, le résultat de `rechercherParTheme` (RG-XIII-01) ; point d'entrée global depuis le tableau de bord de l'accueil (contenu multi-nœuds, comme `PropositionsThemeScreen` du Module XII, pas de nœud éditeur unique à privilégier). « Fiche contenu » (`ContenuMediathequeDetailScreen`, écran 7) combine la fiche détaillée, la bascule favori (écran 4, RG-XIII-04) et les commentaires (écran 5, RG-XIII-03) sur un seul écran, l'auteur des favoris, commentaires et signalements étant la fiche liée à la session (le sélecteur « agir en tant que » a été retiré une fois RG-SEC-01 livré ; un compte sans fiche — utilisateur simple, RG-SEC-06bis — lit sans agir, sans favori, commentaire ni signalement) ; l'ouverture de la fiche enregistre automatiquement une consultation (`enregistrerConsultation`, `addPostFrameCallback` pour éviter d'appeler le dépôt pendant la construction du widget) ; un commentaire masqué (`StatutModerationCommentaire.masque`) est filtré de l'affichage plutôt que montré barré. « Favoris médiathèque » (`FavorisMediathequeScreen`, écran 4, RG-XIII-04) liste les contenus favoris d'un fidèle donné, accessible depuis un bouton de sa fiche (même motif que Discipline/Finances/Comptabilité) ; chaque favori étant indexé uniquement par `contenuId`, l'écran résout chaque contenu individuellement via la nouvelle méthode `MediathequeRepository.watchContenu`. **Aucun écran de création de contenu construit sur mobile** : la production éditoriale reste un écran Windows (« Back-office de publication », non construit), le mobile n'affiche que ce qui a été publié — le seul chemin d'alimentation actif pour l'itération est l'archivage automatique déjà livré au Lot 1 (RG-XIII-02, publication post-culte du Module XII). Test de flux bout-en-bout (`mediatheque_flow_test.dart`) couvrant : création d'un culte, publication avec audio et vidéo (archivage automatique de 2 contenus, RG-XIII-02), catalogue les affichant, bascule favori et dépôt d'un commentaire publié immédiatement (modération a posteriori par défaut, RG-XIII-03), puis vérification depuis l'écran des favoris du fidèle. **Écrans 2 (lecteur), 3 (téléchargements hors ligne) et 9 (gestion de l'espace de stockage local) non construits** — décision de scope « métadonnées seulement » du Lot 1, aucune infra de lecture/téléchargement/stockage dans le dépôt ; **écran 8 (bibliothèque e-books/magazines)** — même limite, la lecture d'un fichier réel (pas seulement son URL) serait nécessaire ; **écran 10 (partage d'un contenu)** — non construit, aucune infra de partage natif (`share_plus` ou équivalent) dans le dépôt ; **écran 11 (statistiques de consultation personnelle)** — `compteurConsultations` existe par contenu (RG-XIII-05 agrégée), mais aucun écran dédié aux statistiques personnelles d'un fidèle n'est construit, différé faute de priorisation. Point d'entrée depuis l'accueil (Catalogue) et depuis la fiche de fidèle (Favoris médiathèque). Reprend les transitions (`sharedAxisPage`) introduites au Module VIII. Éléments différés, chacun avec sa dépendance exacte : **téléversement/lecture/téléchargement de fichiers réels, gestion de l'espace de stockage local** — décision de scope explicite de ce lot, aucune infra de ce type dans le dépôt ; **restriction d'accès réelle par rôle** — ✅ câblée après RG-SEC-01 pour ce qui existe : `MediathequeRules.commentaireVisible` (miroir de la policy `commentaires_lecture` de 0019 — un commentaire en attente de modération n'est visible que de son auteur et des modérateurs, rang pasteur ou plus), favoris d'un fidèle visibles de lui seul (policy `favoris_proprietaire`, bouton de la fiche fidèle limité à sa propre fiche), pas de signalement de son propre commentaire ; **droits de téléchargement (RG-XIII-05)** — sans objet tant qu'aucun téléchargement n'existe (décision « métadonnées seulement ») ; **signalements attribués (RG-XIII-03)** — ✅ livré selon la décision du porteur du projet (2026-09-24) : table `signalements_commentaire(id, commentaire_id, fidele_id, motif, created_at)` (Drift v22), contrainte unique `(commentaire_id, fidele_id)` — un fidèle ne signale qu'une fois un même commentaire, jamais le sien, jamais sans fiche (`MediathequeRules.raisonBlocageSignalement`) ; le masquage automatique compte les **signaleurs distincts** et seulement **depuis la dernière décision de modération** (un commentaire approuvé n'est pas remasqué par les signalements déjà examinés) ; `nombre_signalements` n'est plus qu'une valeur dérivée, recalculée à chaque signalement. Serveur `0025_signalements_moderation_commentaires.sql` : le masquage est fait par un déclencheur `security definer` ; le seuil existe forcément en deux endroits (`AppDefaults.mediathequeSeuilSignalementsAvantMasquage` et `seuil_masquage_commentaire()`) — `seuil_masquage_coherence_test.dart` lit la migration et échoue s'ils divergent ; un signalement ne s'écrit qu'en son nom, sur un commentaire publié d'autrui, sans date future ; le fidèle signalé ne voit pas ses signaleurs, le modérateur si. Constat d'origine corrigé : un seul fidèle masquait n'importe quel commentaire en le signalant trois fois. **Écran de modération** — ✅ livré (`ModerationCommentairesScreen`, accès par une icône du catalogue, rang pasteur, gardé par la route) : commentaires en attente (modération a priori), masqués, ou publiés et signalés depuis la dernière décision, avec l'auteur et le motif de chaque signalement ; approuver publie, rejeter masque ; la décision est **tracée** (`modere_par`, `date_moderation`) et exige une fiche liée (un modérateur sans fiche voit la file, sans pouvoir décider) — vérifiée dans le dépôt (`raisonBlocageModeration`). **Faille de 0019 corrigée à cette occasion** : la policy `commentaires_moderation` laissait un modérateur réécrire toutes les colonnes d'un commentaire (texte, auteur) ; privilèges de colonne limités au statut et à la trace de la décision, `modere_par` imposé au compte réel. Testé par `supabase/tests/0025_signalements_moderation_commentaires_test.sql` (échoue bien avec un compteur brut sans contrainte d'unicité). Limite connue : les dates Drift sont à la seconde ; un signalement fait dans la même seconde qu'une décision de modération n'est pas compté (comparaison stricte — erreur toujours dans le sens « ne pas masquer », sur un commentaire qu'un modérateur vient d'examiner) ; **classement automatique par thème (assistant IA, Module XVI)** — non construit, `theme`/`motsCles` restent saisis manuellement. Synchronisation distante différée pour ce module (même précédent documenté que les modules précédents) |
| XIV — Centre de formation en ligne | ⬜ à faire |
| XVI — Intelligence artificielle | ⬜ à faire |
| XVII — Communication | ⬜ à faire |
| XIX — École biblique | ⬜ à faire |
| XXIV — Bible numérique et vie de l'Église | ⬜ à faire |
| XV — Espace de soutien à GSG | ⬜ à faire |
| XVIII — Événements | ⬜ à faire |
| XXII — Tableau de bord | ⬜ à faire |
| Durcissement et déploiement (Phase 6) | ⬜ à faire |
| **Extension** CV Ecclésiastique (RG-CV-*, sans n° Cahier) | ⬜ à faire — après II et IX |
| **Extension** Cantiques (RG-CANT-*, sans n° Cahier) | ⬜ à faire — après XXIV Palier 1-2 |
| **Extension** Église Sœur (RG-ES-*, sans n° Cahier) | ⬜ à faire — après I et IX |
| i18n (socle ARB, sans `app_strings.dart`) | en continu, dès le premier module |
| Sécurité RG-SEC-05 (helpers `noeuds_du_perimetre()` etc.) | ✅ livré (voir Socle transversal ci-dessus) |

---

## 8. Phases de reconstruction (Annexe D du Cahier)

1. **Fondations** — socle transversal, Module I, Module II, Module XXIII, architecture
   offline-first (RG-OFF), sécurité de base (RG-SEC-01 à 07).
2. **Vie communautaire** — Modules III, IV, V, VI, VII, XII.
3. **Administration et finances** — Modules VIII, IX, X, XI, XX, XXI, intégration mobile money.
4. **Numérique et pédagogie** — Modules XIII, XIV, XVI, XVII, XIX, XXIV (Bible).
5. **Réseau et gouvernance** — Modules XV, XVIII, XXII.
6. **Durcissement et déploiement**.

Extensions hors Cahier (§2-6 de `RECONSTRUCTION_ecclesias360.md`) réappliquées dès que leurs
prérequis de phase existent — voir tableau §7.

---

## 9. Workflow attendu

1. Lire AGENTS.md, puis la section du Cahier concernée.
2. Présenter un plan d'architecture et obtenir validation avant de coder.
3. Implémenter domain → data → application → presentation.
4. `dart run build_runner build --delete-conflicting-outputs` si tables Drift modifiées.
5. `flutter analyze` → No issues found!
6. `flutter test` → tout au vert, un test par règle métier nouvelle.
7. Migration `supabase/migrations/000X_*.sql` si nouvelles données persistées (RLS activée, UUID, timestamps).
8. Commit en français, un commit par module/correction cohérente, **push immédiat vers `origin/main`**.
9. **Amendement du Cahier dans le même commit, systématiquement** (règle permanente, 2026-09-24) :
   toute décision qui crée, précise ou déroge à une règle de gestion (nouvelle RG, exception à
   une RG existante, extension « par analogie », entité ajoutée au modèle de données) est
   reportée dans `Cahier_de_Conception_Ecclesias360_v2.1.md` **dans le commit qui la code** —
   jamais seulement dans AGENTS.md, un commentaire ou la mémoire de session. Précédent à ne pas
   reproduire : RG-XXIV-01, amendé en session avant le sinistre et jamais reporté au Cahier.

---

## 10. Sécurité

- `.env` exclu du dépôt, `.env.example` versionné.
- Jamais logger de clé/jeton/donnée sensible.
- Consentements RG-II-05, données sensibles RG-II-10 via `forbiddenFields`.
- Helpers `security definer` (`fidele_courant_id()`, `role_courant()`, `noeuds_du_perimetre()`) :
  `search_path` durci en `pg_catalog, pg_temp` dès l'écriture initiale (`public` exclu) — voir
  §3 de `RECONSTRUCTION_ecclesias360.md`.
- **Policies RLS actives depuis `0019`** (activation groupée au moment de RG-SEC-01) : le point de
  bascule est passé. Toute table créée ou reconstruite après (Bible, Cantiques, CV, Église Sœur,
  modules restants) livre ses `create policy` **actives dans sa propre migration**, jamais
  commentées, avec son test SQL par exécution réelle (`supabase/tests/`). Réutiliser les gabarits
  et fonctions d'aide de `0019` (`dans_perimetre()`, `fidele_accessible()`, `est_tresorier()`…) ;
  rapport de référence : `supabase/tests/0019_rapport_rls.md`. `dans_perimetre()` inclut
  l'administrateur non borné : une exception nommée (Église Sœur, RG-ES-04, administrateur borné)
  doit appeler `noeuds_du_perimetre()` directement, jamais `dans_perimetre()`.
- **Confidentialité tirée de la source (depuis `0023`)** : un objet qui dérive d'une donnée
  sensible (ex. pièce disciplinaire archivée) suit la règle d'accès de sa source, jamais un
  niveau stocké modifiable. Les colonnes qui portent cette confidentialité sont immuables
  (privilèges de colonne). Toute consultation d'un dossier disciplinaire ou d'une de ses pièces
  passe par une méthode de dépôt qui vérifie **et** journalise (`consulterDossier`,
  `ArchivageRepository.consulter`) — ne jamais afficher une fiche disciplinaire par une simple
  lecture (`findById`) depuis un écran.

---

## 11. Intégration au GSG Platform Kernel — points ouverts

> Le Cahier §8.5 documente la relation d'Ecclésias360 au GSG Platform Kernel v3.0. Cette section
> trace les écarts entre cette cible et l'état du dépôt, et consigne les décisions déjà tranchées
> pour qu'un futur alignement Kernel ne les redéfasse pas.

1. **Référentiel `zones_geographiques` (Module XXIII) — TRANCHÉ, conserver tel quel.**
   Décision : le référentiel `zones_geographiques` (`libelle`, `niveau`, `parent_id`,
   `zone_geo_id` sur `organisation_nodes`) est **conservé**. Ce n'est pas un doublon du GSG
   Referential (pays/devise/langue/ville) : c'est une **zone pastorale interne** (zone,
   district, secteur...) au sens organisationnel du Module XXIII, distincte de l'adresse
   postale d'un nœud.
   Implications :
   - L'adresse d'un nœud, quand elle sera saisie, référencera `pays_id`, `unite_administrative_id`
     et `ville_id` du Kernel — jamais du texte libre, et jamais `zones_geographiques`.
   - `zones_geographiques` ne doit contenir ni pays, ni devise, ni langue, ni ville du Kernel.
   - Le Cahier §8.5 (« Instanciation du Referential Engine ») liste les référentiels internes
     portés en propre par le Module XXIII (dons spirituels, catégories de métiers, natures de
     faute) : les zones géographiques y ont été ajoutées à cette liste (voir Cahier) pour éviter
     toute contradiction lors d'un futur audit Kernel.

2. **Colonnes Kernel absentes du schéma — NON TRANCHÉ, à construire avec leur module porteur.**
   `gsg_org_id` (nœud racine, Org Registry), `langue_id` et `devise_id` (préférences/facturation,
   GSG Referential) ne figurent pas encore dans `organisation_nodes` ni ailleurs. Aucun module
   actuel n'en a besoin pour fonctionner ; à ajouter dès que le module qui les consomme réellement
   est construit (RG-XXIII-05 multilingue par nœud pour `langue_id` ; un futur module de
   facturation/abonnement, Cahier §10, pour `devise_id` et `gsg_org_id`) — ne pas les ajouter à
   vide avant d'avoir un consommateur réel, pour ne pas geler un schéma Kernel non encore stabilisé.

3. **Codes stables pour l'audit — NON TRANCHÉ.**
   `AppError.code` (voir `lib/core/error/app_error.dart`) fournit déjà des codes stables côté
   erreurs applicatives. Le bus d'événements et l'Audit du Kernel (§8.5 du Cahier : « non à ce
   stade ») ne sont pas construits — aucune action requise avant leur construction, mais toute
   future intégration doit transporter `AppError.code` ou équivalent, jamais un message traduit.

---

## 12. UX transversale — accueil, langue/thème, Bible-Cantiques (points ouverts)

> Décisions de conception remontées par le porteur du projet, à ne pas perdre de vue tant que
> les Modules XXIII (paramètres) et XXIV (Bible numérique) ne sont pas construits. Consignées ici
> plutôt que dans le Cahier pour rester au plus près du suivi d'implémentation ; à reporter dans
> le Cahier au moment de construire ces modules si elles impliquent une modification durable de ses
> règles de gestion.

1. **Regroupement des modules par axe sur l'accueil — TRANCHÉ, à appliquer.**
   L'accueil actuel (`lib/features/home/presentation/home_screen.dart`) affiche les modules en
   grille plate, sans distinction. Le Cahier définit déjà 7 axes fonctionnels (§1.1) : Pilier
   hiérarchique, Pilier humain, Axe spirituel, Axe administratif et disciplinaire, Axe financier
   et patrimonial, Axe numérique et pédagogique, Pilier de gouvernance. Décision : l'accueil doit
   regrouper ses tuiles de module par axe (sections avec en-tête), plutôt qu'en grille unique —
   même défaut identifié sur la maquette `ecclesias360_Nouveau` (`dashboard_screen.dart`), à ne
   pas reproduire. Reste à faire : refonte de `home_screen.dart` (aucune dépendance bloquante,
   réalisable dès maintenant avec les 9 modules déjà construits).

2. **L'accueil comme « page de direction » — TRANCHÉ, à construire avec le Module XXIII.**
   Au-delà de la navigation groupée par axe (point 1), l'accueil porte aussi les décisions et
   réglages globaux de l'utilisateur : accès direct à l'espace Bible/Cantiques (Module XXIV) et
   sélecteurs de langue et de thème, plutôt que des réglages enfouis uniquement dans l'écran
   Paramètres (Module XXIII). Cohérent avec RG-SEC-06ter du Cahier (personnalisation de l'accueil
   par widgets, préférences sous les widgets essentiels) et avec l'écran « 2. Choix de langue » déjà
   prévu au Module XXIII. Différé : Module XXIV n'existe pas encore, l'emplacement Bible/Cantiques
   sur l'accueil n'a donc rien à cibler pour l'instant.

3. **Langue et thème choisis à l'accueil influencent l'espace Bible/Cantiques — TRANCHÉ, à
   construire avec les Modules XXIII et XXIV.**
   La langue et le thème ne doivent pas être un réglage local à l'écran Paramètres consommé par lui
   seul : ils doivent être un état global (au niveau de `app.dart` / DI), consommé par tous les
   modules et en premier lieu par la lecture biblique et les cantiques (police, mode sombre/lecture
   déjà prévu au Module XXIV écran 10, langue de la version biblique affichée par défaut). Implique
   que le `ArchivageController`-like controller de préférences (langue/thème) soit instancié au
   niveau racine de l'app, pas dans le sous-arbre du Module XXIII.

4. **Langues locales pour la Bible et les cantiques, en prévision — NON TRANCHÉ, contrainte de
   conception à respecter dès la construction du Module XXIV.**
   RG-XXIV-01 du Cahier (amendé en 2026-09 : Crampon 1923 remplace KJV, Darby et Crampon
   téléchargées à la demande) ne prévoit que 3 traductions libres de droits francophones
   (Louis Segond 1910, Darby 1885, Crampon 1923) ; la KJV, la Reina-Valera 1909 (`SpaRV`) et la
   Bíblia Livre relèvent du volet multilingue, sourcing déjà tranché
   (`RECONSTRUCTION_ecclesias360.md` §4). Ces listes ne doivent pas être figées. Pour
   permettre l'ajout ultérieur d'une langue nationale/locale sans nouvelle version applicative —
   même principe que RG-XXIII-05 pour le paramétrage multilingue général — `VersionBiblique` (et
   son pendant pour les cantiques, non encore modélisé au Cahier) doit être conçu dès le départ
   comme un référentiel extensible en données (table paramétrable), jamais comme un enum Dart figé
   dans le code. À vérifier explicitement au moment de l'architecture du Module XXIV.


---

## 13. Registre — fonctionnalités actées, non encore construites

> Intentions tranchées par le porteur du projet, consignées pour être reprises **après épuisement
> de la liste de modules en cours** (câblage post-RG-SEC-01, puis Phases 4, 5, 6). Aucune ne
> déclenche de code tant qu'elle n'est pas explicitement reprise. Chaque entrée précise ce qui est
> décidé, ce qui reste à réconcilier au moment de la construction, et les dépendances externes.

1. **Accès anonyme en lecture à la Bible (Module XXIV) et aux Cantiques.**
   - Décidé : la lecture (livres / chapitres / versets ; recueils / cantiques) est accessible **sans
     compte**, au même titre que la médiathèque (Module XIII) pour l'utilisateur non affilié.
     Seules les fonctionnalités personnelles — signets, notes, plans de lecture, favoris — exigent
     un compte.
   - **Écart avec la règle source, à signaler et à amender formellement** : RG-SEC-06bis, tel
     qu'écrit au Cahier, ne mentionne que la médiathèque, et pour un utilisateur *authentifié*
     non affilié — pas un accès sans compte. Cette extension doit être inscrite **au Cahier**
     (amendement de RG-SEC-06bis) au moment de la reconstruction de ces modules, jamais seulement
     câblée en code sans mise à jour de la règle.
   - Mécanisme : un motif de préfixes de routes publiques (`_publicPrefixes`) existait avant le
     sinistre pour Bible/Cantiques ; **il n'existe plus dans le dépôt ni dans
     `RECONSTRUCTION_ecclesias360.md`** — à reconstruire. État actuel à faire évoluer : la garde
     `redirectionSession` (`core/router/app_router.dart`) exige une session pour toute route.
   - Côté serveur : les policies de ces tables (déjà actives par défaut depuis `0019`, voir §10)
     devront ouvrir la lecture au rôle `anon` pour le seul contenu biblique/cantiques public — ce
     serait la **première policy `anon` du projet** (0019 n'en contient aucune, invariant vérifié
     par son test) : l'exception devra être nommée et testée comme telle, jamais généralisée.

2. **Page d'accueil repensée comme page de décision.**
   - Décidé : après l'onboarding, l'utilisateur atterrit sur un accueil qui sert de hub de
     navigation et d'action — paramètres globaux (langue, thème), accès direct à la lecture
     Bible / Cantiques / paroles de chants — plutôt que directement sur un tableau de bord
     personnalisé. Prolonge §12 points 1 à 3 (regroupement par axe, accueil « page de direction »,
     langue/thème globaux influençant Bible/Cantiques), qui restent valables.
   - **À réconcilier au moment de la construction** : RG-SEC-06ter (déjà au Cahier) décrit, pour
     l'utilisateur authentifié, un accueil personnalisé par widgets (cultes à venir, contributions
     récentes, contenus recommandés, puis préférences). La page de décision doit être conçue
     **compatible** avec RG-SEC-06ter une fois connecté — par exemple hub en tête, widgets
     personnels ensuite — et non comme un remplacement qui l'annule.
   - Articulation avec l'entrée 1 : sans compte, la page de décision est aussi le point d'entrée
     de la lecture anonyme Bible/Cantiques.

3. **Écran d'authentification : e-mail par défaut, connexion par compte Google.**
   - E-mail en premier (au lieu du téléphone, ordre actuel de `ConnexionScreen`) : simple choix
     d'interface, sans incidence sur les règles.
   - **Connexion Google : extension réelle au-delà de RG-SEC-01 tel que construit** (aujourd'hui
     téléphone/e-mail + OTP / Magic Link uniquement, aucun fournisseur OAuth tiers). Nécessite la
     configuration d'un fournisseur OAuth Google dans Supabase (identifiants Google Cloud : client
     OAuth, écran de consentement, URI de redirection par plateforme) — **dépendance
     d'infrastructure externe**, à traiter avec la même prudence que GSG ID (RG-SEC-01bis, reporté)
     ou Twilio (non configuré) : rien n'est construit tant que ces identifiants ne sont pas fournis
     et vérifiables de bout en bout.
   - À décider au moment de la construction : la liaison compte ↔ fiche (`LiaisonCompteRules`)
     s'appuie sur un identifiant **vérifié** ; l'e-mail d'un compte Google est vérifié par Google —
     confirmer que la correspondance automatique et le garde-fou « fiche déjà liée » s'appliquent à
     l'identique, et que ce fournisseur ne crée pas de second compte pour une même personne déjà
     inscrite par OTP (fusion ou refus à trancher).

4. **Explication contextuelle d'un passage biblique sélectionné par l'IA.**
   - Décidé : s'inscrit dans le périmètre de l'assistant IA (Module XVI), en particulier
     **RG-XVI-02** (toute réponse générée à partir de la Bible numérique référence sa source
     consultée, affichée pour vérification). Fonctionnalité **opt-in**, activable dans les
     paramètres, **jamais active par défaut**. Valeurs IA via `assets/config/ai_roles.json` (§5,
     §6 — rôle à nommer dans le pool fermé, probablement Logos ou Charmeine, réservés au Module XVI),
     jamais en dur.
   - **Point à vérifier au moment de la construction** : RG-XVI-03 prévoit un mode dégradé hors
     connexion sur un sous-ensemble embarqué (versets, définitions courantes) et la mise en file
     d'attente des requêtes cloud. Une explication de passage étant généralement plus riche, elle
     nécessitera probablement un appel cloud : la documenter **comme telle** (file d'attente hors
     connexion, pas de réponse immédiate), plutôt que la présumer disponible hors ligne.
   - Garde-fous déjà applicables (§6) : suggestion uniquement, jamais d'écriture en base ;
     RG-XVI-01 (n'interroge que ce à quoi l'utilisateur a accès).

5. **Procédure d'exercice du droit d'accès aux données personnelles (RGPD / loi nationale).**
   - Décidé (2026-09-24, avec RG-II-11) : le refus applicatif d'afficher au fidèle concerné les
     notes pastorales écrites sur lui n'éteint pas son droit d'accès légal. Ce droit s'exerce par
     une procédure administrative encadrée (demande, vérification d'identité, export par
     l'administrateur), **chantier séparé, hors code pour l'instant** — ne bloque pas le Module II.
   - À construire avec l'écran 14 du Module II / Consentement RGPD (Module XXIII, RG-II-10) et le
     veilleur Uriel (§6) ; à cadrer juridiquement (législation applicable par pays de
     déploiement) avant tout code. L'export devra être tracé comme une consultation.
